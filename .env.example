# Klucz do Anthropic API — potrzebny tylko dla werdyktu AI (/api/analyze).
# Bez klucza aplikacja działa normalnie na werdykcie statycznym.
# Na Vercelu dodaj w: Project Settings -> Environment Variables
ANTHROPIC_API_KEY=
